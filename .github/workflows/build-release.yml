name: Build And Release

on:
  push:
    branches:
      - main
  workflow_dispatch:
    inputs:
      prerelease:
        description: "Mark as pre-release"
        required: false
        default: "false"

permissions:
  contents: write
  actions: read
  issues: read

env:
  PYTHON_VERSION: "3.11"
  APP_NAME: Face-Local
  OWNER: ${{ github.repository_owner }}
  ARTIFACT_REPO: ${{ github.repository_owner }}/private_artifact
  # Sparkle 2 — pin the version for reproducible builds.
  # Update this variable and rotate SPARKLE_PUBLIC_KEY / SPARKLE_PRIVATE_KEY
  # whenever upgrading Sparkle.
  SPARKLE_VERSION: "2.6.4"

jobs:
  pick-runner:
    name: Pick build runner
    runs-on:
      - self-hosted
      - Linux
      - ARM64
    outputs:
      mac_runner: ${{ steps.pick_mac.outputs.runner }}
      mac_runner_note: ${{ steps.pick_mac.outputs.runner_note }}
      windows_runner: ${{ steps.pick_windows.outputs.runner }}
      windows_runner_note: ${{ steps.pick_windows.outputs.runner_note }}
    steps:
      - name: Pick macOS runner
        id: pick_mac
        env:
          GH_TOKEN: ${{ secrets.RUNNER_ADMIN_TOKEN }}
          RUNNER_NAME: Koncsik-MacBook-Air
        shell: bash
        run: |
          PYTHON_BIN="$(command -v python3 || command -v python || true)"
          STATUS=""
          if [ -n "$PYTHON_BIN" ]; then
            STATUS=$("$PYTHON_BIN" - <<'PY' 2>/dev/null || true
          import json
          import os
          import urllib.request

          token = os.environ.get("GH_TOKEN", "")
          if not token:
              raise SystemExit(0)

          request = urllib.request.Request(
              f"https://api.github.com/orgs/{os.environ['OWNER']}/actions/runners",
              headers={
                  "Accept": "application/vnd.github+json",
                  "Authorization": f"Bearer {token}",
                  "X-GitHub-Api-Version": "2022-11-28",
              },
          )
          with urllib.request.urlopen(request) as response:
              payload = json.load(response)

          for runner in payload.get("runners", []):
              if runner.get("name") == os.environ["RUNNER_NAME"]:
                  print(runner.get("status", ""))
                  break
          PY
            )
          fi

          if [ "$STATUS" = "online" ]; then
            echo 'runner=["self-hosted", "macOS", "ARM64"]' >> "$GITHUB_OUTPUT"
            echo 'runner_note=Runner: Koncsik-MacBook-Air (online)' >> "$GITHUB_OUTPUT"
          else
            echo 'runner=["macos-latest"]' >> "$GITHUB_OUTPUT"
            echo 'runner_note=Runner: macos-latest (fallback)' >> "$GITHUB_OUTPUT"
          fi

      - name: Pick Windows runner
        id: pick_windows
        env:
          GH_TOKEN: ${{ secrets.RUNNER_ADMIN_TOKEN }}
          RUNNER_NAME: nagy_gep_windows
        shell: bash
        run: |
          PYTHON_BIN="$(command -v python3 || command -v python || true)"
          STATUS=""
          if [ -n "$PYTHON_BIN" ]; then
            STATUS=$("$PYTHON_BIN" - <<'PY' 2>/dev/null || true
          import json
          import os
          import urllib.request

          token = os.environ.get("GH_TOKEN", "")
          if not token:
              raise SystemExit(0)

          request = urllib.request.Request(
              f"https://api.github.com/orgs/{os.environ['OWNER']}/actions/runners",
              headers={
                  "Accept": "application/vnd.github+json",
                  "Authorization": f"Bearer {token}",
                  "X-GitHub-Api-Version": "2022-11-28",
              },
          )
          with urllib.request.urlopen(request) as response:
              payload = json.load(response)

          for runner in payload.get("runners", []):
              if runner.get("name") == os.environ["RUNNER_NAME"]:
                  print(runner.get("status", ""))
                  break
          PY
            )
          fi

          if [ "$STATUS" = "online" ]; then
            echo 'runner=["self-hosted", "Windows", "X64"]' >> "$GITHUB_OUTPUT"
            echo 'runner_note=Runner: nagy_gep_windows (online)' >> "$GITHUB_OUTPUT"
          else
            echo 'runner=["windows-latest"]' >> "$GITHUB_OUTPUT"
            echo 'runner_note=Runner: windows-latest (fallback)' >> "$GITHUB_OUTPUT"
          fi

  prepare:
    runs-on:
      - self-hosted
      - Linux
      - ARM64
    outputs:
      base_version: ${{ steps.meta.outputs.base_version }}
      package_version: ${{ steps.meta.outputs.package_version }}
      tag: ${{ steps.meta.outputs.tag }}
      release_name: ${{ steps.meta.outputs.release_name }}
    steps:
      - uses: actions/checkout@v6

      - uses: actions/setup-python@v6
        with:
          python-version: ${{ env.PYTHON_VERSION }}

      - id: meta
        shell: bash
        run: |
          git fetch --tags --force
          export BASE_VERSION="$(python - <<'PY'
          import os
          import re
          import subprocess
          import tomllib

          with open("pyproject.toml", "rb") as fh:
              configured = tomllib.load(fh)["project"]["version"]

          match = re.fullmatch(r"(\d+)\.(\d+)\.(\d+)", configured)
          if not match:
              raise SystemExit(f"pyproject version must be semver x.y.z, got: {configured}")

          major, minor, patch_floor = map(int, match.groups())
          prefix = f"v{major}.{minor}."
          tags = subprocess.check_output(["git", "tag", "--list", f"{prefix}*"], text=True).splitlines()

          patch_numbers = []
          for tag in tags:
              tag_match = re.fullmatch(rf"v{major}\.{minor}\.(\d+)", tag.strip())
              if tag_match:
                  patch_numbers.append(int(tag_match.group(1)))

          next_patch = patch_floor if not patch_numbers else max(max(patch_numbers) + 1, patch_floor)
          print(f"{major}.{minor}.{next_patch}")
          PY
          )"
          PACKAGE_VERSION="$BASE_VERSION"
          TAG="v${PACKAGE_VERSION}"
          RELEASE_NAME="Face-Local v${PACKAGE_VERSION}"

          {
            echo "base_version=$BASE_VERSION"
            echo "package_version=$PACKAGE_VERSION"
            echo "tag=$TAG"
            echo "release_name=$RELEASE_NAME"
          } >> "$GITHUB_OUTPUT"

  build-macos:
    needs:
      - prepare
      - pick-runner
    runs-on: ${{ fromJSON(needs.pick-runner.outputs.mac_runner) }}
    steps:
      - uses: actions/checkout@v6

      - name: Selected macOS runner
        shell: bash
        run: echo "${{ needs.pick-runner.outputs.mac_runner_note }}"

      - name: Prepare writable Python tool cache
        if: ${{ !contains(needs.pick-runner.outputs.mac_runner, 'self-hosted') }}
        shell: bash
        run: |
          TOOLCACHE="${RUNNER_TEMP}/hostedtoolcache"
          mkdir -p "$TOOLCACHE"
          echo "AGENT_TOOLSDIRECTORY=$TOOLCACHE" >> "$GITHUB_ENV"
          echo "RUNNER_TOOL_CACHE=$TOOLCACHE" >> "$GITHUB_ENV"

      - name: Select local Python on self-hosted macOS
        if: ${{ contains(needs.pick-runner.outputs.mac_runner, 'self-hosted') }}
        shell: bash
        run: |
          find_python() {
            for candidate in python3.13 python3.12 python3.11 python3 python; do
              local bin
              bin="$(command -v "$candidate" 2>/dev/null)" || continue
              if "$bin" - <<'PY' >/dev/null 2>&1
          import sys
          raise SystemExit(0 if sys.version_info >= (3, 11) else 1)
          PY
              then
                echo "$bin"
                return 0
              fi
            done
            # also check well-known Homebrew locations not always on PATH
            for candidate in \
              /opt/homebrew/bin/python3.13 \
              /opt/homebrew/bin/python3.12 \
              /opt/homebrew/bin/python3.11 \
              /usr/local/bin/python3.13 \
              /usr/local/bin/python3.12 \
              /usr/local/bin/python3.11; do
              [ -x "$candidate" ] || continue
              if "$candidate" - <<'PY' >/dev/null 2>&1
          import sys
          raise SystemExit(0 if sys.version_info >= (3, 11) else 1)
          PY
              then
                echo "$candidate"
                return 0
              fi
            done
            return 1
          }

          PY_BIN="$(find_python)" || {
            echo "Python 3.11+ not found — installing via Homebrew..." >&2
            brew install python@3.11 >&2
            PY_BIN="$(find_python)" || {
              echo "Python 3.11+ was not found even after Homebrew install." >&2
              exit 1
            }
          }

          VENV_DIR="${RUNNER_TEMP}/venv"
          "$PY_BIN" -m venv "$VENV_DIR"
          echo "$VENV_DIR/bin" >> "$GITHUB_PATH"
          echo "MACOS_PYTHON_BIN=$PY_BIN" >> "$GITHUB_ENV"
          "$PY_BIN" --version

      - uses: actions/setup-python@v6
        if: ${{ !contains(needs.pick-runner.outputs.mac_runner, 'self-hosted') }}
        with:
          python-version: ${{ env.PYTHON_VERSION }}
          cache: pip
          cache-dependency-path: |
            pyproject.toml
            requirements.txt

      - name: Install Python dependencies
        shell: bash
        run: |
          python -m pip install --upgrade pip setuptools wheel
          python -m pip install . pyinstaller

      - name: Download embedding models
        shell: bash
        run: |
          mkdir -p models
          if [ ! -f models/mobilefacenet.tflite ]; then
            mirrors=(
              "https://github.com/MCarlomagno/FaceRecognitionAuth/raw/refs/heads/master/assets/mobilefacenet.tflite"
              "https://github.com/pb-julian/liteface/raw/main/tflite_models/mobilefacenet.tflite"
              "https://github.com/shubham0204/FaceRecognition_With_FaceNet_Android/raw/master/app/src/main/assets/mobile_face_net.tflite"
            )
            for url in "${mirrors[@]}"; do
              if curl -fsSL --retry 3 -o models/mobilefacenet.tflite "$url"; then
                echo "Downloaded mobilefacenet.tflite from $url"
                break
              fi
            done
            [ -f models/mobilefacenet.tflite ] || { echo "ERROR: Failed to download mobilefacenet.tflite"; exit 1; }
          fi
          if [ ! -f models/face_recognition_sface_2021dec.onnx ]; then
            curl -fsSL --retry 3 \
              -o models/face_recognition_sface_2021dec.onnx \
              "https://github.com/opencv/opencv_zoo/raw/main/models/face_recognition_sface/face_recognition_sface_2021dec.onnx" \
              || echo "WARNING: SFace model download failed (non-fatal)"
          fi

      - name: Build bundled app
        shell: bash
        run: python scripts/package_app.py --version "${{ needs.prepare.outputs.package_version }}"

      # ──────────────────────────────────────────────────────────────────────
      # Sparkle 2 integration
      # ──────────────────────────────────────────────────────────────────────

      - name: Download Sparkle distribution
        shell: bash
        run: |
          set -euo pipefail
          DIST_DIR="${RUNNER_TEMP}/sparkle-dist"
          mkdir -p "$DIST_DIR"
          URL="https://github.com/sparkle-project/Sparkle/releases/download/${SPARKLE_VERSION}/Sparkle-${SPARKLE_VERSION}.tar.xz"
          echo "Downloading Sparkle ${SPARKLE_VERSION} from $URL"
          curl -fsSL --retry 3 "$URL" | tar -xJ -C "$DIST_DIR"
          echo "SPARKLE_DIST=$DIST_DIR" >> "$GITHUB_ENV"
          ls -la "$DIST_DIR"

      - name: Compile SparkleHelper
        shell: bash
        run: |
          set -euo pipefail
          if ! command -v swiftc >/dev/null 2>&1; then
            echo "::error::swiftc not found — Xcode must be installed on the macOS runner"
            exit 1
          fi
          ARCH="$(uname -m)"
          TARGET="${ARCH}-apple-macos12.0"
          echo "Compiling SparkleHelper for $TARGET"
          swiftc \
            -O \
            -target "$TARGET" \
            -F "$SPARKLE_DIST" \
            -framework Sparkle \
            -o "dist/SparkleHelper" \
            sparkle/Sources/SparkleHelper/main.swift
          file dist/SparkleHelper

      - name: Bundle Sparkle framework and helper into app
        shell: bash
        run: |
          set -euo pipefail
          APP="dist/Face-Local.app"
          mkdir -p "$APP/Contents/Frameworks"

          # Replace or copy Sparkle.framework
          rm -rf "$APP/Contents/Frameworks/Sparkle.framework"
          cp -R "$SPARKLE_DIST/Sparkle.framework" "$APP/Contents/Frameworks/"

          # Install SparkleHelper binary
          cp "dist/SparkleHelper" "$APP/Contents/MacOS/SparkleHelper"
          chmod +x "$APP/Contents/MacOS/SparkleHelper"

          echo "Bundle contents:"
          ls -la "$APP/Contents/Frameworks/"
          ls -la "$APP/Contents/MacOS/"

      - name: Patch Info.plist with version and Sparkle keys
        shell: bash
        env:
          PACKAGE_VERSION: ${{ needs.prepare.outputs.package_version }}
          SPARKLE_PUBLIC_KEY: ${{ secrets.SPARKLE_PUBLIC_KEY }}
        run: |
          APPCAST_URL="https://raw.githubusercontent.com/${{ github.repository }}/main/docs/appcast.xml"
          export APPCAST_URL
          python3 - <<'PY'
          import plistlib, os, sys
          from pathlib import Path
          from datetime import datetime

          plist_path = Path("dist/Face-Local.app/Contents/Info.plist")
          if not plist_path.exists():
              print(f"ERROR: {plist_path} not found", file=sys.stderr)
              sys.exit(1)

          with open(plist_path, "rb") as fh:
              plist = plistlib.load(fh)

          version      = os.environ["PACKAGE_VERSION"]
          su_key       = os.environ.get("SPARKLE_PUBLIC_KEY", "")
          appcast_url  = os.environ.get("APPCAST_URL", "")

          plist["CFBundleShortVersionString"] = version
          plist["CFBundleVersion"]            = version
          plist["NSHumanReadableCopyright"]   = f"Copyright © {datetime.now().year} Face-Local"
          plist["NSHighResolutionCapable"]    = True

          if su_key:
              plist["SUPublicEDKey"] = su_key
              print(f"  SUPublicEDKey: {su_key[:24]}...")
          else:
              print("  WARNING: SPARKLE_PUBLIC_KEY not set — update checking disabled in this build")
          if appcast_url:
              plist["SUFeedURL"] = appcast_url
              print(f"  SUFeedURL: {appcast_url}")

          with open(plist_path, "wb") as fh:
              plistlib.dump(plist, fh)
          print(f"Info.plist patched (version={version})")
          PY

      # ──────────────────────────────────────────────────────────────────────
      # Code signing — conditional on DEVELOPER_ID_CERT_P12 secret
      # ──────────────────────────────────────────────────────────────────────

      - name: Setup keychain and import Developer ID certificate
        if: ${{ env.CERT_P12 != '' }}
        env:
          CERT_P12: ${{ secrets.DEVELOPER_ID_CERT_P12 }}
          CERT_PASSWORD: ${{ secrets.DEVELOPER_ID_CERT_PASSWORD }}
        shell: bash
        run: |
          set -euo pipefail
          KEYCHAIN_PATH="${RUNNER_TEMP}/build.keychain"
          KEYCHAIN_PASSWORD="$(openssl rand -hex 16)"
          echo "KEYCHAIN_PATH=$KEYCHAIN_PATH" >> "$GITHUB_ENV"

          security create-keychain -p "$KEYCHAIN_PASSWORD" "$KEYCHAIN_PATH"
          security default-keychain -s "$KEYCHAIN_PATH"
          security unlock-keychain  -p "$KEYCHAIN_PASSWORD" "$KEYCHAIN_PATH"
          security set-keychain-settings -t 3600 -u "$KEYCHAIN_PATH"

          CERT_FILE="${RUNNER_TEMP}/developer_id.p12"
          echo "$CERT_P12" | base64 --decode > "$CERT_FILE"
          security import "$CERT_FILE" -k "$KEYCHAIN_PATH" \
            -P "$CERT_PASSWORD" -T /usr/bin/codesign -T /usr/bin/security
          rm -f "$CERT_FILE"

          security set-key-partition-list -S apple-tool:,apple: \
            -s -k "$KEYCHAIN_PASSWORD" "$KEYCHAIN_PATH"

          IDENTITY="$(security find-identity -v -p codesigning "$KEYCHAIN_PATH" \
            | grep "Developer ID Application" | head -1 | sed 's/.*"\(.*\)".*/\1/')"
          if [ -z "$IDENTITY" ]; then
            echo "::error::Developer ID Application certificate not found in keychain"
            exit 1
          fi
          echo "SIGN_IDENTITY=$IDENTITY" >> "$GITHUB_ENV"
          echo "Signing identity resolved: $IDENTITY"

      - name: Sign macOS app bundle
        if: ${{ env.SIGN_IDENTITY != '' }}
        shell: bash
        run: |
          set -euo pipefail
          APP="dist/Face-Local.app"
          ENTITLEMENTS="assets/entitlements.plist"
          echo "=== Signing $APP ==="

          # 1. Sign Python extension modules — no hardened runtime (they load dynamically)
          echo "--- Signing .so / .dylib ---"
          find "$APP" -type f \( -name "*.so" -o -name "*.dylib" \) \
            | while IFS= read -r f; do
                codesign --force --sign "$SIGN_IDENTITY" "$f" 2>/dev/null || true
              done

          # 2. Sign any bare Python binary
          find "$APP" -type f -name "Python" \
            | while IFS= read -r f; do
                codesign --force --options runtime --sign "$SIGN_IDENTITY" "$f" 2>/dev/null || true
              done

          # 3. Sparkle XPC services (innermost-first)
          SPARKLE_FW="$APP/Contents/Frameworks/Sparkle.framework"
          if [ -d "$SPARKLE_FW" ]; then
            for xpc in "$SPARKLE_FW"/Versions/*/XPCServices/*.xpc; do
              [ -e "$xpc" ] || continue
              codesign --force --options runtime \
                --entitlements "$ENTITLEMENTS" --sign "$SIGN_IDENTITY" "$xpc"
            done
            for updater in "$SPARKLE_FW"/Versions/*/Updater.app; do
              [ -e "$updater" ] || continue
              codesign --force --deep --options runtime \
                --entitlements "$ENTITLEMENTS" --sign "$SIGN_IDENTITY" "$updater"
            done
            # Main Sparkle dylib
            SPARKLE_LIB="$SPARKLE_FW/Sparkle"
            [ -f "$SPARKLE_LIB" ] || SPARKLE_LIB="$SPARKLE_FW/Versions/Current/Sparkle"
            codesign --force --sign "$SIGN_IDENTITY" "$SPARKLE_LIB"
            codesign --force --sign "$SIGN_IDENTITY" "$SPARKLE_FW"
          fi

          # 4. SparkleHelper with hardened runtime
          HELPER="$APP/Contents/MacOS/SparkleHelper"
          [ -f "$HELPER" ] && codesign --force --options runtime \
            --sign "$SIGN_IDENTITY" "$HELPER"

          # 5. Main executable with hardened runtime + entitlements
          codesign --force --options runtime \
            --entitlements "$ENTITLEMENTS" --sign "$SIGN_IDENTITY" \
            "$APP/Contents/MacOS/Face-Local"

          # 6. App bundle
          codesign --force --options runtime \
            --entitlements "$ENTITLEMENTS" --sign "$SIGN_IDENTITY" "$APP"

          echo "=== Verifying ==="
          codesign --verify --deep --strict --verbose=2 "$APP"
          echo "Signing complete."

      - name: Notarize app
        if: ${{ env.SIGN_IDENTITY != '' && env.ASC_API_KEY_PRIVATE_KEY != '' }}
        env:
          ASC_API_KEY_PRIVATE_KEY: ${{ secrets.ASC_API_KEY_PRIVATE_KEY }}
          ASC_API_KEY_ID: ${{ secrets.ASC_API_KEY_ID }}
          ASC_API_KEY_ISSUER_ID: ${{ secrets.ASC_API_KEY_ISSUER_ID }}
          APPLE_TEAM_ID: ${{ secrets.APPLE_TEAM_ID }}
        shell: bash
        run: |
          set -euo pipefail
          echo "=== Notarizing ==="

          ASC_KEY_FILE="${RUNNER_TEMP}/asc_api_key.p8"
          echo "$ASC_API_KEY_PRIVATE_KEY" | base64 --decode > "$ASC_KEY_FILE"

          NOTARIZE_ZIP="${RUNNER_TEMP}/Face-Local-notarize.zip"
          ditto -c -k --sequesterRsrc --keepParent \
            "dist/Face-Local.app" "$NOTARIZE_ZIP"

          xcrun notarytool submit "$NOTARIZE_ZIP" \
            --key          "$ASC_KEY_FILE" \
            --key-id       "$ASC_API_KEY_ID" \
            --issuer       "$ASC_API_KEY_ISSUER_ID" \
            --team-id      "$APPLE_TEAM_ID" \
            --wait --timeout 1200 --verbose

          rm -f "$ASC_KEY_FILE" "$NOTARIZE_ZIP"
          echo "NOTARIZED=true" >> "$GITHUB_ENV"
          echo "Notarization successful."

      - name: Staple notarization ticket to app
        if: ${{ env.NOTARIZED == 'true' }}
        shell: bash
        run: |
          xcrun stapler staple   "dist/Face-Local.app"
          xcrun stapler validate "dist/Face-Local.app"
          echo "Stapling complete."

      # ──────────────────────────────────────────────────────────────────────
      # Package artifacts
      # ──────────────────────────────────────────────────────────────────────

      - name: Create release directory
        shell: bash
        run: mkdir -p release

      - name: Package macOS DMG
        shell: bash
        run: |
          rm -rf release/dmg-root
          mkdir -p release/dmg-root
          cp -R "dist/Face-Local.app" "release/dmg-root/"
          ln -s /Applications "release/dmg-root/Applications"
          hdiutil create \
            -volname "Face-Local" \
            -srcfolder "release/dmg-root" \
            -ov \
            -format UDZO \
            "release/Face-Local-macOS-installer-${{ needs.prepare.outputs.package_version }}.dmg"
          rm -rf release/dmg-root

      - name: Create Sparkle ZIP
        shell: bash
        run: |
          # Sparkle requires the .app at the ZIP root.  ditto preserves
          # extended attributes, resource forks, and symlinks.
          ditto -c -k --sequesterRsrc --keepParent \
            "dist/Face-Local.app" \
            "release/Face-Local-macOS-sparkle-${{ needs.prepare.outputs.package_version }}.zip"
          ls -lh "release/Face-Local-macOS-sparkle-${{ needs.prepare.outputs.package_version }}.zip"

      - name: Sign Sparkle ZIP with EdDSA
        if: ${{ env.SPARKLE_PRIVATE_KEY != '' }}
        env:
          SPARKLE_PRIVATE_KEY: ${{ secrets.SPARKLE_PRIVATE_KEY }}
        shell: bash
        run: |
          set -euo pipefail
          ZIP="release/Face-Local-macOS-sparkle-${{ needs.prepare.outputs.package_version }}.zip"

          # Write key text directly — Sparkle sign_update expects the raw
          # base64 private key string as-is, not binary.
          KEY_FILE="${RUNNER_TEMP}/sparkle_ed_private_key"
          printf '%s' "$SPARKLE_PRIVATE_KEY" > "$KEY_FILE"
          chmod 600 "$KEY_FILE"

          EDDSA_SIG="$("$SPARKLE_DIST/bin/sign_update" "$ZIP" --ed-key-file "$KEY_FILE")"
          rm -f "$KEY_FILE"
          echo "EdDSA signature: $EDDSA_SIG"
          printf 'SPARKLE_EDDSA_SIG<<%s\n%s\n%s\n' "EOF_SIG" "$EDDSA_SIG" "EOF_SIG" >> "$GITHUB_ENV"

          EDDSA_SIG="$EDDSA_SIG" python3 - <<'PY'
          import json, os
          from pathlib import Path
          meta = {
              "version":        "${{ needs.prepare.outputs.package_version }}",
              "build":          "${{ needs.prepare.outputs.package_version }}",
              "zip_name":       "Face-Local-macOS-sparkle-${{ needs.prepare.outputs.package_version }}.zip",
              "eddsa_signature": os.environ["EDDSA_SIG"],
              "min_os_version": "12.0",
              "signed":         True,
          }
          Path("release/sparkle_meta.json").write_text(json.dumps(meta, indent=2))
          print("sparkle_meta.json written")
          PY

      - name: Write unsigned Sparkle metadata
        if: ${{ env.SPARKLE_PRIVATE_KEY == '' }}
        env:
          SPARKLE_PRIVATE_KEY: ${{ secrets.SPARKLE_PRIVATE_KEY }}
        shell: bash
        run: |
          python3 - <<'PY'
          import json
          from pathlib import Path
          meta = {
              "version":         "${{ needs.prepare.outputs.package_version }}",
              "build":           "${{ needs.prepare.outputs.package_version }}",
              "zip_name":        "Face-Local-macOS-sparkle-${{ needs.prepare.outputs.package_version }}.zip",
              "eddsa_signature": "",
              "min_os_version":  "12.0",
              "signed":          False,
          }
          Path("release/sparkle_meta.json").write_text(json.dumps(meta, indent=2))
          print("WARNING: SPARKLE_PRIVATE_KEY not set — appcast will have empty EdDSA signature")
          PY

      - name: Upload macOS artifact
        id: upload_macos
        continue-on-error: true
        uses: actions/upload-artifact@v4
        with:
          name: face-local-macos-${{ needs.prepare.outputs.tag }}
          path: |
            release/*.dmg
            release/*.zip
            release/sparkle_meta.json
          if-no-files-found: error

      - name: Fallback upload macOS artifact to private repo
        if: ${{ steps.upload_macos.outcome == 'failure' }}
        env:
          GH_TOKEN: ${{ secrets.PRIVATE_ARTIFACT_TOKEN }}
        shell: bash
        run: |
          set -euo pipefail
          export PATH="/opt/homebrew/bin:/usr/local/bin:$PATH"
          if ! command -v gh >/dev/null 2>&1; then
            command -v brew >/dev/null 2>&1 || {
              echo "gh CLI not found and Homebrew is unavailable." >&2
              exit 1
            }
            brew install gh
          fi

          shopt -s nullglob
          assets=(release/*.dmg release/*.zip)
          if [ "${#assets[@]}" -eq 0 ]; then
            echo "No macOS artifact found in release/." >&2
            exit 1
          fi

          tag="artifact-release-${{ needs.prepare.outputs.package_version }}"
          if ! gh release view "$tag" --repo "${{ env.ARTIFACT_REPO }}" >/dev/null 2>&1; then
            gh release create "$tag" \
              --repo "${{ env.ARTIFACT_REPO }}" \
              --title "CI artifact ${{ needs.prepare.outputs.tag }}" \
              --notes "CI artifact fallback [run ${{ github.run_number }}]" \
              --prerelease \
              || gh release view "$tag" --repo "${{ env.ARTIFACT_REPO }}" >/dev/null
          fi

          gh release upload "$tag" "${assets[@]}" release/sparkle_meta.json \
            --repo "${{ env.ARTIFACT_REPO }}" \
            --clobber

  build-windows:
    needs:
      - prepare
      - pick-runner
    runs-on: ${{ fromJSON(needs.pick-runner.outputs.windows_runner) }}
    steps:
      - uses: actions/checkout@v6

      - name: Selected Windows runner
        shell: powershell
        run: echo "${{ needs.pick-runner.outputs.windows_runner_note }}"

      - name: Select local Python on self-hosted Windows
        if: ${{ contains(needs.pick-runner.outputs.windows_runner, 'self-hosted') }}
        shell: powershell
        run: |
          $candidates = @(
            "python", "python3",
            "C:\Python313\python.exe", "C:\Python312\python.exe", "C:\Python311\python.exe",
            "$env:LOCALAPPDATA\Programs\Python\Python313\python.exe",
            "$env:LOCALAPPDATA\Programs\Python\Python312\python.exe",
            "$env:LOCALAPPDATA\Programs\Python\Python311\python.exe",
            "C:\Program Files\Python313\python.exe",
            "C:\Program Files\Python312\python.exe",
            "C:\Program Files\Python311\python.exe"
          )
          $pyBin = $null
          foreach ($c in $candidates) {
            $bin = if ($c -notmatch '[\\/]') {
              $cmd = Get-Command $c -ErrorAction SilentlyContinue | Select-Object -First 1
              if ($cmd) { $cmd.Source } else { $null }
            } elseif (Test-Path $c) { $c } else { $null }
            if (-not $bin) { continue }
            & $bin -c "import sys; raise SystemExit(0 if sys.version_info >= (3,11) else 1)" 2>$null
            if ($LASTEXITCODE -eq 0) { $pyBin = $bin; break }
          }
          $venvDir = Join-Path $env:RUNNER_TEMP "face-local-venv"
          if (Test-Path $venvDir) { Remove-Item $venvDir -Recurse -Force }

          if ($pyBin) {
            Write-Host "Using local Python: $pyBin"
            & $pyBin -m venv $venvDir
            if ($LASTEXITCODE -ne 0) { Write-Error "Failed to create Python virtual environment"; exit 1 }
          } else {
            Write-Host "Python 3.11+ not found on self-hosted runner; bootstrapping a local Python with uv."
            [Net.ServicePointManager]::SecurityProtocol = [Net.SecurityProtocolType]::Tls12
            $uvRoot = Join-Path $env:RUNNER_TEMP "uv"
            $uvZip = Join-Path $env:RUNNER_TEMP "uv.zip"
            if (Test-Path $uvRoot) { Remove-Item $uvRoot -Recurse -Force }
            New-Item -ItemType Directory -Force -Path $uvRoot | Out-Null
            Invoke-WebRequest -UseBasicParsing -Uri "https://github.com/astral-sh/uv/releases/latest/download/uv-x86_64-pc-windows-msvc.zip" -OutFile $uvZip
            Expand-Archive -LiteralPath $uvZip -DestinationPath $uvRoot -Force
            $uv = Get-ChildItem $uvRoot -Filter "uv.exe" -Recurse | Select-Object -First 1 -ExpandProperty FullName
            if (-not $uv) { Write-Error "uv.exe was not found after extraction"; exit 1 }
            $env:UV_CACHE_DIR = Join-Path $env:RUNNER_TEMP "uv-cache"
            $env:UV_PYTHON_INSTALL_DIR = Join-Path $env:RUNNER_TEMP "uv-python"
            & $uv --version
            & $uv python install 3.11
            if ($LASTEXITCODE -ne 0) { Write-Error "uv failed to install Python 3.11"; exit 1 }
            & $uv venv --seed $venvDir --python 3.11
            if ($LASTEXITCODE -ne 0) { Write-Error "uv failed to create Python virtual environment"; exit 1 }
          }

          Add-Content $env:GITHUB_PATH (Join-Path $venvDir "Scripts")
          Add-Content $env:GITHUB_ENV "VIRTUAL_ENV=$venvDir"
          $venvPython = Join-Path $venvDir "Scripts\python.exe"
          & $venvPython --version
          & $venvPython -m pip --version
          if ($LASTEXITCODE -ne 0) { Write-Error "pip is not available in the Python virtual environment"; exit 1 }

      - uses: actions/setup-python@v6
        if: ${{ !contains(needs.pick-runner.outputs.windows_runner, 'self-hosted') }}
        with:
          python-version: ${{ env.PYTHON_VERSION }}
          cache: pip
          cache-dependency-path: |
            pyproject.toml
            requirements.txt

      - name: Install Python dependencies
        shell: powershell
        run: |
          python -m pip install --upgrade pip setuptools wheel
          python -m pip install . pyinstaller
          # Verify TFLite backend installed (ai-edge-litert is in base deps, this is a safety check)
          # Use Continue so Python stderr doesn't become a terminating NativeCommandError under $ErrorActionPreference=Stop
          $litert_ok = $false
          $ErrorActionPreference = 'Continue'
          python -c "from ai_edge_litert.interpreter import Interpreter; print('ai-edge-litert OK')" 2>&1 | Out-String | Write-Host
          if ($LASTEXITCODE -eq 0) { $litert_ok = $true }
          $ErrorActionPreference = 'Stop'
          if (-not $litert_ok) {
            Write-Warning "ai-edge-litert not available for this Python version. Trying explicit install..."
            python -m pip install "ai-edge-litert>=1.0" --quiet
            $ErrorActionPreference = 'Continue'
            python -c "from ai_edge_litert.interpreter import Interpreter; print('ai-edge-litert OK (explicit)')" 2>&1 | Out-String | Write-Host
            if ($LASTEXITCODE -eq 0) { $litert_ok = $true }
            $ErrorActionPreference = 'Stop'
            if (-not $litert_ok) {
              Write-Warning "ai-edge-litert still not available. Build will use SFace/HOG fallback for embeddings."
            }
          }

      - name: Download embedding models
        shell: powershell
        run: |
          New-Item -ItemType Directory -Force -Path models | Out-Null
          $facenet = "models\mobilefacenet.tflite"
          if (-not (Test-Path $facenet)) {
            $mirrors = @(
              "https://github.com/MCarlomagno/FaceRecognitionAuth/raw/refs/heads/master/assets/mobilefacenet.tflite",
              "https://github.com/pb-julian/liteface/raw/main/tflite_models/mobilefacenet.tflite",
              "https://github.com/shubham0204/FaceRecognition_With_FaceNet_Android/raw/master/app/src/main/assets/mobile_face_net.tflite"
            )
            $ok = $false
            foreach ($url in $mirrors) {
              try {
                Invoke-WebRequest -Uri $url -OutFile $facenet -UseBasicParsing -ErrorAction Stop
                Write-Host "Downloaded mobilefacenet.tflite from $url"
                $ok = $true
                break
              } catch { Write-Warning "Mirror failed: $url" }
            }
            if (-not $ok) { throw "ERROR: Failed to download mobilefacenet.tflite from all mirrors" }
          }
          $sface = "models\face_recognition_sface_2021dec.onnx"
          if (-not (Test-Path $sface)) {
            try {
              Invoke-WebRequest -Uri "https://github.com/opencv/opencv_zoo/raw/main/models/face_recognition_sface/face_recognition_sface_2021dec.onnx" -OutFile $sface -UseBasicParsing -ErrorAction Stop
            } catch { Write-Warning "SFace model download failed (non-fatal)" }
          }

      - name: Build bundled app
        shell: powershell
        run: python scripts/package_app.py --version "${{ needs.prepare.outputs.package_version }}"

      - name: Create release directory
        shell: powershell
        run: New-Item -ItemType Directory -Force -Path release | Out-Null

      - name: Package Windows assets
        shell: powershell
        run: |
          Compress-Archive -Path "dist\Face-Local\*" -DestinationPath "release\Face-Local-windows-portable-${{ needs.prepare.outputs.package_version }}.zip" -Force

          # Portable Inno Setup — plain zip extraction, no installer process, no admin rights.
          $innoVersion = "6.7.3"
          $installDir = Join-Path $env:RUNNER_TEMP "InnoSetup6"
          $isccCandidates = @(
            "C:\Program Files (x86)\Inno Setup 6\ISCC.exe",
            "C:\Program Files\Inno Setup 6\ISCC.exe",
            (Join-Path $installDir "tools\ISCC.exe"),
            (Join-Path $installDir "ISCC.exe")
          )
          function Find-ISCC { $isccCandidates | Where-Object { Test-Path $_ } | Select-Object -First 1 }

          $iscc = Find-ISCC
          if (-not $iscc) {
            Write-Host "Inno Setup 6 not found - downloading the portable build (Tools.InnoSetup $innoVersion from NuGet)."
            try {
              [Net.ServicePointManager]::SecurityProtocol = [Net.SecurityProtocolType]::Tls12
              # .nupkg is a plain zip; the .zip extension is required by Expand-Archive.
              $isZip = Join-Path $env:RUNNER_TEMP "tools.innosetup.$innoVersion.zip"
              if (Test-Path $isZip) { Remove-Item $isZip -Force }
              if (Test-Path $installDir) { Remove-Item $installDir -Recurse -Force }
              Invoke-WebRequest -UseBasicParsing `
                -Uri "https://api.nuget.org/v3-flatcontainer/tools.innosetup/$innoVersion/tools.innosetup.$innoVersion.nupkg" `
                -OutFile $isZip
              Expand-Archive -LiteralPath $isZip -DestinationPath $installDir -Force
              # Strip any mark-of-the-web so ISCC and its DLLs are not blocked.
              Get-ChildItem $installDir -Recurse -File | Unblock-File -ErrorAction SilentlyContinue
              Remove-Item $isZip -ErrorAction SilentlyContinue
            } catch { Write-Warning "Portable Inno Setup download failed: $_" }
            $iscc = Find-ISCC
          }

          if (-not $iscc) {
            Write-Host "Falling back to the official Inno Setup installer (per-user, no admin required)."
            try {
              [Net.ServicePointManager]::SecurityProtocol = [Net.SecurityProtocolType]::Tls12
              $isTmp = Join-Path $env:RUNNER_TEMP "innosetup-installer.exe"
              Invoke-WebRequest -UseBasicParsing -Uri "https://jrsoftware.org/download.php/is.exe" -OutFile $isTmp
              Unblock-File $isTmp -ErrorAction SilentlyContinue
              if (Test-Path $installDir) { Remove-Item $installDir -Recurse -Force }
              $proc = Start-Process -FilePath $isTmp `
                -ArgumentList "/VERYSILENT","/SUPPRESSMSGBOXES","/NORESTART","/SP-","/CURRENTUSER","/DIR=$installDir" `
                -Wait -PassThru
              Write-Host "Inno Setup installer exited with code $($proc.ExitCode)"
              Remove-Item $isTmp -ErrorAction SilentlyContinue
            } catch { Write-Warning "Direct Inno Setup download/install failed: $_" }
            $iscc = Find-ISCC
          }

          if (-not $iscc) {
            Write-Error "Inno Setup 6 (ISCC.exe) could not be found or installed. Pre-install Inno Setup 6 on this runner (https://jrsoftware.org/isdl.php), or check that nuget.org is reachable from it."
            exit 1
          }
          Write-Host "Using ISCC: $iscc"
          & $iscc "/DAppVersion=${{ needs.prepare.outputs.package_version }}" "scripts\build_windows_installer.iss"
          if ($LASTEXITCODE -ne 0) { Write-Error "ISCC exited with code $LASTEXITCODE"; exit $LASTEXITCODE }

      - name: Upload Windows artifact
        id: upload_windows
        continue-on-error: true
        uses: actions/upload-artifact@v4
        with:
          name: face-local-windows-${{ needs.prepare.outputs.tag }}
          path: |
            release/*.zip
            release/*.exe
          if-no-files-found: error

      - name: Fallback upload Windows artifact to private repo
        if: ${{ steps.upload_windows.outcome == 'failure' }}
        env:
          GH_TOKEN: ${{ secrets.PRIVATE_ARTIFACT_TOKEN }}
        shell: powershell
        run: |
          if (-not (Get-Command gh -ErrorAction SilentlyContinue)) {
            $ErrorActionPreference = "Stop"
            $GhVersion = (Invoke-RestMethod "https://api.github.com/repos/cli/cli/releases/latest").tag_name.TrimStart('v')
            $GhZip = Join-Path $env:RUNNER_TEMP "gh_windows_amd64.zip"
            $GhDir = Join-Path $env:RUNNER_TEMP "gh-bin"
            Invoke-WebRequest -Uri "https://github.com/cli/cli/releases/download/v${GhVersion}/gh_${GhVersion}_windows_amd64.zip" -OutFile $GhZip
            Expand-Archive -Path $GhZip -DestinationPath $GhDir -Force
            $GhExe = Get-ChildItem -Path $GhDir -Filter "gh.exe" -Recurse | Select-Object -First 1
            if ($null -eq $GhExe) { throw "gh.exe not found after extraction in $GhDir" }
            $env:PATH = "$($GhExe.DirectoryName);$env:PATH"
          }

          $Assets = @()
          $Assets += Get-ChildItem -Path "release" -Filter "*.zip" -File | ForEach-Object { $_.FullName }
          $Assets += Get-ChildItem -Path "release" -Filter "*.exe" -File | ForEach-Object { $_.FullName }
          if ($Assets.Count -eq 0) { throw "No Windows artifacts found in release/." }

          $Tag = "artifact-release-${{ needs.prepare.outputs.package_version }}"

          $ErrorActionPreference = "SilentlyContinue"
          $null = gh release view $Tag --repo "${{ env.ARTIFACT_REPO }}" 2>&1
          $releaseExists = ($LASTEXITCODE -eq 0)
          $ErrorActionPreference = "Stop"

          if (-not $releaseExists) {
            gh release create $Tag `
              --repo "${{ env.ARTIFACT_REPO }}" `
              --title "CI artifact ${{ needs.prepare.outputs.tag }}" `
              --notes "CI artifact fallback [run ${{ github.run_number }}]" `
              --prerelease
            if ($LASTEXITCODE -ne 0) {
              gh release view $Tag --repo "${{ env.ARTIFACT_REPO }}"
              if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }
            }
          }

          gh release upload $Tag @Assets `
            --repo "${{ env.ARTIFACT_REPO }}" `
            --clobber
          if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }

  build-linux-arm64:
    needs: prepare
    runs-on:
      - self-hosted
      - Linux
      - ARM64
    steps:
      - uses: actions/checkout@v6

      - uses: actions/setup-python@v6
        with:
          python-version: ${{ env.PYTHON_VERSION }}
          cache: pip
          cache-dependency-path: |
            pyproject.toml
            requirements.txt

      - name: Install Linux packaging dependencies
        shell: bash
        run: |
          set -euo pipefail
          packages=(dpkg-dev libgl1 libglib2.0-0 libxkbcommon-x11-0 libxcb-cursor0)
          missing=()
          for package in "${packages[@]}"; do
            if ! dpkg-query -W -f='${Status}' "$package" 2>/dev/null | grep -q "install ok installed"; then
              missing+=("$package")
            fi
          done

          if [ "${#missing[@]}" -eq 0 ]; then
            echo "Linux packaging dependencies are already installed."
          elif command -v sudo >/dev/null 2>&1 && sudo -n true 2>/dev/null; then
            sudo apt-get update
            sudo apt-get install -y "${missing[@]}"
          else
            echo "::warning::Missing Linux packages: ${missing[*]}. This self-hosted runner cannot run sudo without a password, so continuing without apt install. Install these packages once on the runner if the build fails."
          fi

      - name: Install Python dependencies
        shell: bash
        run: |
          python -m pip install --upgrade pip setuptools wheel
          python -m pip install . pyinstaller

      - name: Download embedding models
        shell: bash
        run: |
          mkdir -p models
          if [ ! -f models/mobilefacenet.tflite ]; then
            mirrors=(
              "https://github.com/MCarlomagno/FaceRecognitionAuth/raw/refs/heads/master/assets/mobilefacenet.tflite"
              "https://github.com/pb-julian/liteface/raw/main/tflite_models/mobilefacenet.tflite"
              "https://github.com/shubham0204/FaceRecognition_With_FaceNet_Android/raw/master/app/src/main/assets/mobile_face_net.tflite"
            )
            for url in "${mirrors[@]}"; do
              if curl -fsSL --retry 3 -o models/mobilefacenet.tflite "$url"; then
                echo "Downloaded mobilefacenet.tflite from $url"
                break
              fi
            done
            [ -f models/mobilefacenet.tflite ] || { echo "ERROR: Failed to download mobilefacenet.tflite"; exit 1; }
          fi
          if [ ! -f models/face_recognition_sface_2021dec.onnx ]; then
            curl -fsSL --retry 3 \
              -o models/face_recognition_sface_2021dec.onnx \
              "https://github.com/opencv/opencv_zoo/raw/main/models/face_recognition_sface/face_recognition_sface_2021dec.onnx" \
              || echo "WARNING: SFace model download failed (non-fatal)"
          fi

      - name: Build bundled app
        shell: bash
        run: python scripts/package_app.py --version "${{ needs.prepare.outputs.package_version }}"

      - name: Create release directory
        shell: bash
        run: mkdir -p release

      - name: Package Linux installer
        shell: bash
        run: |
          chmod +x scripts/build_linux_deb.sh
          scripts/build_linux_deb.sh "${{ needs.prepare.outputs.package_version }}"

      - name: Upload Linux ARM64 artifact
        id: upload_linux_arm64
        continue-on-error: true
        uses: actions/upload-artifact@v4
        with:
          name: face-local-linux-arm64-${{ needs.prepare.outputs.tag }}
          path: |
            release/*.deb
            release/*.tar.gz
          if-no-files-found: error

      - name: Fallback upload Linux ARM64 artifact to private repo
        if: ${{ steps.upload_linux_arm64.outcome == 'failure' }}
        env:
          GH_TOKEN: ${{ secrets.PRIVATE_ARTIFACT_TOKEN }}
        shell: bash
        run: |
          set -euo pipefail
          if ! command -v gh >/dev/null 2>&1; then
            echo "gh CLI not found. Install GitHub CLI on this runner for artifact fallback uploads." >&2
            exit 1
          fi

          shopt -s nullglob
          assets=(release/*.deb release/*.tar.gz)
          if [ "${#assets[@]}" -eq 0 ]; then
            echo "No Linux ARM64 artifacts found in release/." >&2
            exit 1
          fi

          tag="artifact-release-${{ needs.prepare.outputs.package_version }}"
          if ! gh release view "$tag" --repo "${{ env.ARTIFACT_REPO }}" >/dev/null 2>&1; then
            gh release create "$tag" \
              --repo "${{ env.ARTIFACT_REPO }}" \
              --title "CI artifact ${{ needs.prepare.outputs.tag }}" \
              --notes "CI artifact fallback [run ${{ github.run_number }}]" \
              --prerelease \
              || gh release view "$tag" --repo "${{ env.ARTIFACT_REPO }}" >/dev/null
          fi

          gh release upload "$tag" "${assets[@]}" \
            --repo "${{ env.ARTIFACT_REPO }}" \
            --clobber

  build-linux-x64:
    needs: prepare
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6

      - uses: actions/setup-python@v6
        with:
          python-version: ${{ env.PYTHON_VERSION }}
          cache: pip
          cache-dependency-path: |
            pyproject.toml
            requirements.txt

      - name: Install Linux packaging dependencies
        shell: bash
        run: |
          sudo apt-get update
          sudo apt-get install -y dpkg-dev libgl1 libglib2.0-0 libxkbcommon-x11-0 libxcb-cursor0

      - name: Install Python dependencies
        shell: bash
        run: |
          python -m pip install --upgrade pip setuptools wheel
          python -m pip install . pyinstaller

      - name: Download embedding models
        shell: bash
        run: |
          mkdir -p models
          if [ ! -f models/mobilefacenet.tflite ]; then
            mirrors=(
              "https://github.com/MCarlomagno/FaceRecognitionAuth/raw/refs/heads/master/assets/mobilefacenet.tflite"
              "https://github.com/pb-julian/liteface/raw/main/tflite_models/mobilefacenet.tflite"
              "https://github.com/shubham0204/FaceRecognition_With_FaceNet_Android/raw/master/app/src/main/assets/mobile_face_net.tflite"
            )
            for url in "${mirrors[@]}"; do
              if curl -fsSL --retry 3 -o models/mobilefacenet.tflite "$url"; then
                echo "Downloaded mobilefacenet.tflite from $url"
                break
              fi
            done
            [ -f models/mobilefacenet.tflite ] || { echo "ERROR: Failed to download mobilefacenet.tflite"; exit 1; }
          fi
          if [ ! -f models/face_recognition_sface_2021dec.onnx ]; then
            curl -fsSL --retry 3 \
              -o models/face_recognition_sface_2021dec.onnx \
              "https://github.com/opencv/opencv_zoo/raw/main/models/face_recognition_sface/face_recognition_sface_2021dec.onnx" \
              || echo "WARNING: SFace model download failed (non-fatal)"
          fi

      - name: Build bundled app
        shell: bash
        run: python scripts/package_app.py --version "${{ needs.prepare.outputs.package_version }}"

      - name: Create release directory
        shell: bash
        run: mkdir -p release

      - name: Package Linux x64 installer
        shell: bash
        run: |
          chmod +x scripts/build_linux_deb.sh
          scripts/build_linux_deb.sh "${{ needs.prepare.outputs.package_version }}"

      - name: Upload Linux x64 artifact
        id: upload_linux_x64
        continue-on-error: true
        uses: actions/upload-artifact@v4
        with:
          name: face-local-linux-x64-${{ needs.prepare.outputs.tag }}
          path: |
            release/*.deb
            release/*.tar.gz
          if-no-files-found: error

      - name: Fallback upload Linux x64 artifact to private repo
        if: ${{ steps.upload_linux_x64.outcome == 'failure' }}
        env:
          GH_TOKEN: ${{ secrets.PRIVATE_ARTIFACT_TOKEN }}
        shell: bash
        run: |
          set -euo pipefail
          if ! command -v gh >/dev/null 2>&1; then
            echo "gh CLI not found. Install GitHub CLI on this runner for artifact fallback downloads." >&2
            exit 1
          fi

          shopt -s nullglob
          assets=(release/*.deb release/*.tar.gz)
          if [ "${#assets[@]}" -eq 0 ]; then
            echo "No Linux x64 artifacts found in release/." >&2
            exit 1
          fi

          tag="artifact-release-${{ needs.prepare.outputs.package_version }}"
          if ! gh release view "$tag" --repo "${{ env.ARTIFACT_REPO }}" >/dev/null 2>&1; then
            gh release create "$tag" \
              --repo "${{ env.ARTIFACT_REPO }}" \
              --title "CI artifact ${{ needs.prepare.outputs.tag }}" \
              --notes "CI artifact fallback [run ${{ needs.prepare.outputs.package_version }}]" \
              --prerelease \
              || gh release view "$tag" --repo "${{ env.ARTIFACT_REPO }}" >/dev/null
          fi

          gh release upload "$tag" "${assets[@]}" \
            --repo "${{ env.ARTIFACT_REPO }}" \
            --clobber

  release:
    if: >-
      ${{
        always() &&
        (
          needs.build-macos.result == 'success' ||
          needs.build-windows.result == 'success' ||
          needs.build-linux-arm64.result == 'success' ||
          needs.build-linux-x64.result == 'success'
        )
      }}
    needs:
      - prepare
      - pick-runner
      - build-macos
      - build-windows
      - build-linux-arm64
      - build-linux-x64
    runs-on:
      - self-hosted
      - Linux
      - ARM64
    steps:
      - uses: actions/checkout@v6
        with:
          # Fine-grained PAT needed to bypass branch protection for appcast.xml commit
          token: ${{ secrets.APPCAST_PUSH_TOKEN }}

      - name: Download release artifacts
        env:
          GITHUB_TOKEN: ${{ github.token }}
          PRIVATE_ARTIFACT_TOKEN: ${{ secrets.PRIVATE_ARTIFACT_TOKEN }}
          PACKAGE_VERSION: ${{ needs.prepare.outputs.package_version }}
          RELEASE_TAG: ${{ needs.prepare.outputs.tag }}
          BUILD_MACOS_RESULT: ${{ needs.build-macos.result }}
          BUILD_WINDOWS_RESULT: ${{ needs.build-windows.result }}
          BUILD_LINUX_ARM64_RESULT: ${{ needs.build-linux-arm64.result }}
          BUILD_LINUX_X64_RESULT: ${{ needs.build-linux-x64.result }}
        shell: bash
        run: |
          set -euo pipefail
          mkdir -p release

          python3 - <<'PY'
          import fnmatch
          import http.client
          import json
          import os
          import sys
          import time
          import urllib.error
          import urllib.parse
          import urllib.request
          import zipfile
          from pathlib import Path

          release_dir = Path("release")
          release_dir.mkdir(exist_ok=True)
          download_dir = release_dir / ".downloads"
          download_dir.mkdir(exist_ok=True)

          repo = os.environ["GITHUB_REPOSITORY"]
          run_id = os.environ["GITHUB_RUN_ID"]
          github_token = os.environ["GITHUB_TOKEN"]
          private_token = os.environ.get("PRIVATE_ARTIFACT_TOKEN", "")
          artifact_repo = os.environ["ARTIFACT_REPO"]
          package_version = os.environ["PACKAGE_VERSION"]
          release_tag = os.environ["RELEASE_TAG"]
          fallback_tag = f"artifact-release-{package_version}"

          class SafeRedirectHandler(urllib.request.HTTPRedirectHandler):
            def redirect_request(self, req, fp, code, msg, headers, newurl):
              redirected = super().redirect_request(req, fp, code, msg, headers, newurl)
              if redirected is None:
                return None
              old_host = urllib.parse.urlparse(req.full_url).netloc
              new_host = urllib.parse.urlparse(newurl).netloc
              if old_host != new_host:
                redirected.remove_header("Authorization")
              return redirected

          opener = urllib.request.build_opener(SafeRedirectHandler())

          def request_json(url, token):
            req = urllib.request.Request(
              url,
              headers={
                "Accept": "application/vnd.github+json",
                "Authorization": f"Bearer {token}",
                "X-GitHub-Api-Version": "2022-11-28",
              },
            )
            with opener.open(req, timeout=120) as response:
              return json.load(response)

          def request_download(url, token, accept):
            return urllib.request.Request(
              url,
              headers={
                "Accept": accept,
                "Authorization": f"Bearer {token}",
                "X-GitHub-Api-Version": "2022-11-28",
              },
            )

          def should_retry(exc):
            if isinstance(exc, http.client.IncompleteRead):
              return True
            if isinstance(exc, TimeoutError):
              return True
            if isinstance(exc, urllib.error.HTTPError):
              return exc.code in {408, 429, 500, 502, 503, 504}
            if isinstance(exc, urllib.error.URLError):
              return True
            return False

          def download_file(url, token, target, accept="application/octet-stream", attempts=4):
            target = Path(target)
            tmp_target = target.with_suffix(target.suffix + ".part")
            last_exc = None

            for attempt in range(1, attempts + 1):
              try:
                req = request_download(url, token, accept)
                with opener.open(req, timeout=900) as response, tmp_target.open("wb") as fh:
                  expected_header = response.headers.get("Content-Length")
                  expected_size = int(expected_header) if expected_header else None
                  bytes_written = 0
                  while True:
                    chunk = response.read(1024 * 1024)
                    if not chunk:
                      break
                    fh.write(chunk)
                    bytes_written += len(chunk)
                  if expected_size is not None and bytes_written != expected_size:
                    missing = max(0, expected_size - bytes_written)
                    partial = f"{bytes_written} bytes".encode()
                    raise http.client.IncompleteRead(partial, missing)
                tmp_target.replace(target)
                return
              except Exception as exc:  # noqa: BLE001 - this is a CI download retry loop
                last_exc = exc
                tmp_target.unlink(missing_ok=True)
                if attempt == attempts or not should_retry(exc):
                  break
                wait_seconds = min(60, 5 * attempt)
                print(
                  f"Download failed on attempt {attempt}/{attempts}: {exc}; "
                  f"retrying in {wait_seconds}s",
                  flush=True,
                )
                time.sleep(wait_seconds)

            raise RuntimeError(f"download failed after {attempts} attempt(s): {last_exc}") from last_exc

          def download_workflow_artifact(name):
            print(f"Downloading workflow artifact: {name}", flush=True)
            url = f"https://api.github.com/repos/{repo}/actions/runs/{run_id}/artifacts?per_page=100"
            artifacts = request_json(url, github_token).get("artifacts", [])
            artifact = next((item for item in artifacts if item.get("name") == name), None)
            if artifact is None:
              raise RuntimeError(f"workflow artifact not found: {name}")

            archive_path = download_dir / f"{name}.zip"
            download_file(
              artifact["archive_download_url"],
              github_token,
              archive_path,
              accept="application/vnd.github+json",
            )
            with zipfile.ZipFile(archive_path) as zf:
              zf.extractall(release_dir)
            archive_path.unlink(missing_ok=True)
            print(f"{name}: workflow artifact downloaded", flush=True)

          def list_release_assets():
            if not private_token:
              raise RuntimeError("PRIVATE_ARTIFACT_TOKEN is empty; cannot use private artifact fallback")

            release = request_json(
              f"https://api.github.com/repos/{artifact_repo}/releases/tags/{fallback_tag}",
              private_token,
            )
            return release.get("assets", [])

          def fallback_release_download(pattern, required=True):
            assets = list_release_assets()
            matches = [asset for asset in assets if fnmatch.fnmatch(asset.get("name", ""), pattern)]
            if not matches and required:
              raise RuntimeError(f"fallback release asset not found: {pattern}")
            if not matches:
              print(f"Optional fallback asset not found: {pattern}", flush=True)
              return

            for asset in matches:
              target = release_dir / asset["name"]
              download_file(asset["url"], private_token, target)
              print(f"Downloaded fallback asset: {asset['name']}", flush=True)

          def download_platform(label, artifact_name, required_patterns, optional_patterns=()):
            try:
              download_workflow_artifact(artifact_name)
              return
            except Exception as exc:
              print(f"{label}: workflow artifact download failed: {exc}", flush=True)
              if not private_token:
                raise RuntimeError(
                  "workflow artifact download failed and PRIVATE_ARTIFACT_TOKEN "
                  "is empty; cannot use private artifact fallback"
                ) from exc
              print(f"{label}: trying private artifact repo fallback", flush=True)

            for pattern in required_patterns:
              fallback_release_download(pattern, required=True)
            for pattern in optional_patterns:
              fallback_release_download(pattern, required=False)

          platforms = []
          if os.environ["BUILD_MACOS_RESULT"] == "success":
            platforms.append((
              "macOS",
              f"face-local-macos-{release_tag}",
              [f"Face-Local-macOS-installer-{package_version}.dmg"],
              [f"Face-Local-macOS-sparkle-{package_version}.zip", "sparkle_meta.json"],
            ))
          if os.environ["BUILD_WINDOWS_RESULT"] == "success":
            platforms.append((
              "Windows",
              f"face-local-windows-{release_tag}",
              [f"Face-Local-windows-*-{package_version}.*"],
              [],
            ))
          if os.environ["BUILD_LINUX_ARM64_RESULT"] == "success":
            platforms.append((
              "Linux ARM64",
              f"face-local-linux-arm64-{release_tag}",
              [f"Face-Local-linux-arm64-*-{package_version}.*"],
              [],
            ))
          if os.environ["BUILD_LINUX_X64_RESULT"] == "success":
            platforms.append((
              "Linux x64",
              f"face-local-linux-x64-{release_tag}",
              [f"Face-Local-linux-x64-*-{package_version}.*"],
              [],
            ))

          failures = []
          for label, artifact_name, required, optional in platforms:
            try:
              download_platform(label, artifact_name, required, optional)
            except Exception as exc:
              failures.append(f"{label}: {exc}")

          if failures:
            for failure in failures:
              print(failure, file=sys.stderr)
            raise SystemExit(1)
          PY

      - name: Verify release assets
        shell: bash
        run: |
          set -euo pipefail
          version="${{ needs.prepare.outputs.package_version }}"
          missing=0

          require_file() {
            local path="$1"
            if [ ! -f "$path" ]; then
              echo "Required artifact not found: $path" >&2
              missing=1
            fi
          }

          if [ "${{ needs.build-macos.result }}" = "success" ]; then
            require_file "release/Face-Local-macOS-installer-${version}.dmg"
          fi
          if [ "${{ needs.build-windows.result }}" = "success" ]; then
            require_file "release/Face-Local-windows-installer-${version}.exe"
          fi
          if [ "${{ needs.build-linux-arm64.result }}" = "success" ]; then
            require_file "release/Face-Local-linux-arm64-installer-${version}.deb"
            require_file "release/Face-Local-linux-arm64-portable-${version}.tar.gz"
          fi
          if [ "${{ needs.build-linux-x64.result }}" = "success" ]; then
            require_file "release/Face-Local-linux-x64-installer-${version}.deb"
            require_file "release/Face-Local-linux-x64-portable-${version}.tar.gz"
          fi

          if [ "$missing" -ne 0 ]; then
            ls -la release || true
            exit 1
          fi

      # ──────────────────────────────────────────────────────────────────────
      # Generate and publish appcast.xml (stable URL for Sparkle)
      # ──────────────────────────────────────────────────────────────────────

      - name: Generate appcast.xml
        if: ${{ needs.build-macos.result == 'success' }}
        shell: bash
        run: |
          set -euo pipefail
          VERSION="${{ needs.prepare.outputs.package_version }}"
          TAG="${{ needs.prepare.outputs.tag }}"
          ZIP="release/Face-Local-macOS-sparkle-${VERSION}.zip"
          META="release/sparkle_meta.json"

          if [ ! -f "$ZIP" ]; then
            echo "Sparkle ZIP not found at $ZIP — skipping appcast generation"
            exit 0
          fi

          EDDSA_SIG=""
          if [ -f "$META" ]; then
            EDDSA_SIG="$(python3 -c "import json; print(json.load(open('$META'))['eddsa_signature'])")"
          fi

          if [ -z "$EDDSA_SIG" ]; then
            echo "::warning::EdDSA signature is empty — appcast will be unsigned"
            EDDSA_SIG="UNSIGNED"
          fi

          DOWNLOAD_URL="https://github.com/${{ github.repository }}/releases/download/${TAG}/Face-Local-macOS-sparkle-${VERSION}.zip"
          NOTES_URL="https://github.com/${{ github.repository }}/releases/tag/${TAG}"

          python3 scripts/generate_appcast.py \
            --version "$VERSION" \
            --build   "$VERSION" \
            --zip     "$ZIP" \
            --signature "$EDDSA_SIG" \
            --download-url     "$DOWNLOAD_URL" \
            --release-notes-url "$NOTES_URL" \
            --output docs/appcast.xml

          echo "=== Generated docs/appcast.xml ==="
          cat docs/appcast.xml

      - name: Commit and push appcast.xml
        if: ${{ needs.build-macos.result == 'success' }}
        shell: bash
        run: |
          set -euo pipefail
          if [ ! -f docs/appcast.xml ]; then
            echo "appcast.xml not generated — skipping commit"
            exit 0
          fi
          # Skip if still a placeholder (no <item> element means no real release)
          if ! grep -q "<item>" docs/appcast.xml; then
            echo "appcast.xml contains no release item — skipping commit"
            exit 0
          fi

          git config user.name  "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"

          VERSION="${{ needs.prepare.outputs.package_version }}"

          # Stash our freshly generated appcast; we re-apply it on top of the
          # latest origin/main each attempt instead of rebasing (the generator
          # rewrites the whole file, so a rebase would always conflict when a
          # newer release pushed while this — possibly older — workflow ran).
          cp docs/appcast.xml /tmp/appcast.new.xml

          for attempt in 1 2 3; do
            git fetch origin main
            git reset --hard origin/main

            # Never downgrade the Sparkle feed: if origin/main already advertises
            # a newer release than ours, a stale/parallel workflow must not roll it
            # back to an older version. Skip cleanly (success, not error).
            REMOTE_VER="$(grep -oP 'sparkle:shortVersionString="\K[0-9.]+' docs/appcast.xml | head -1 || true)"
            if [ -n "$REMOTE_VER" ]; then
              NEWEST="$(printf '%s\n%s\n' "$REMOTE_VER" "$VERSION" | sort -V | tail -1)"
              if [ "$NEWEST" != "$VERSION" ]; then
                echo "origin/main already has appcast for $REMOTE_VER (newer than $VERSION) — skipping downgrade"
                exit 0
              fi
            fi

            cp /tmp/appcast.new.xml docs/appcast.xml
            git add docs/appcast.xml
            if git diff --cached --quiet; then
              echo "appcast.xml already up to date on origin/main — nothing to commit"
              exit 0
            fi

            git commit -m "chore: update appcast.xml for v${VERSION} [skip ci]"

            if git push origin HEAD:main; then
              echo "appcast.xml pushed"
              exit 0
            fi

            echo "Push rejected (attempt $attempt) — re-applying onto fresh origin/main and retrying..."
          done

          echo "Push failed after 3 attempts" >&2
          exit 1

      - name: Upload assets to GitHub release
        env:
          GITHUB_TOKEN: ${{ secrets.APPCAST_PUSH_TOKEN || github.token }}
        shell: bash
        run: |
          python3 scripts/github_release.py upload \
            --repo "$GITHUB_REPOSITORY" \
            --tag "${{ needs.prepare.outputs.tag }}" \
            --target "$GITHUB_SHA" \
            --name "${{ needs.prepare.outputs.release_name }}" \
            "release/*.dmg" \
            "release/*.zip" \
            "release/*.exe" \
            "release/*.deb" \
            "release/*.tar.gz"

      - name: Update GitHub release notes
        env:
          GITHUB_TOKEN: ${{ secrets.APPCAST_PUSH_TOKEN || github.token }}
          RELEASE_NAME: ${{ needs.prepare.outputs.release_name }}
          RELEASE_TAG: ${{ needs.prepare.outputs.tag }}
        shell: bash
        run: |
          git fetch --tags --force --unshallow || git fetch --tags --force
          notes="$(python3 scripts/github_release.py build-notes \
            --repo "$GITHUB_REPOSITORY" \
            --tag "$RELEASE_TAG" \
            --target "$GITHUB_SHA" \
            --run-url "${GITHUB_SERVER_URL}/${GITHUB_REPOSITORY}/actions/runs/${GITHUB_RUN_ID}" \
            --runner-note "macOS runner: ${{ needs.pick-runner.outputs.mac_runner_note }}" \
            --runner-note "Windows runner: ${{ needs.pick-runner.outputs.windows_runner_note }}" \
            --build-result "macOS build: ${{ needs.build-macos.result }}" \
            --build-result "Windows build: ${{ needs.build-windows.result }}" \
            --build-result "Linux ARM64 build: ${{ needs.build-linux-arm64.result }}" \
            --build-result "Linux x64 build on GitHub runner: ${{ needs.build-linux-x64.result }}")"
          python3 scripts/github_release.py update-notes \
            --repo "$GITHUB_REPOSITORY" \
            --tag "$RELEASE_TAG" \
            --target "$GITHUB_SHA" \
            --name "$RELEASE_NAME" \
            --notes "$notes"

  post-to-buffer:
    if: ${{ needs.release.result == 'success' && github.event_name == 'push' && github.run_attempt == 1 }}
    needs:
      - prepare
      - build-macos
      - build-windows
      - build-linux-arm64
      - build-linux-x64
      - release
    runs-on:
      - self-hosted
      - Linux
      - ARM64
    steps:
      - uses: actions/checkout@v6

      - name: Post release announcement via Buffer
        continue-on-error: true
        env:
          RELEASE_TAG: ${{ needs.prepare.outputs.tag }}
          BUFFER_API_KEY: ${{ secrets.BUFFER_API_KEY }}
          BUFFER_ORGANIZATION_ID: ${{ secrets.BUFFER_ORGANIZATION_ID }}
          BUFFER_CHANNEL_ID: ${{ secrets.BUFFER_CHANNEL_ID }}
          BUFFER_CHANNEL_NAME: ${{ secrets.BUFFER_CHANNEL_NAME }}
          BUFFER_CHANNEL_SERVICE: ${{ secrets.BUFFER_CHANNEL_SERVICE }}
          BUFFER_POST_MODE: ${{ secrets.BUFFER_POST_MODE }}
          BUFFER_POST_TEMPLATE: ${{ secrets.BUFFER_POST_TEMPLATE }}
        shell: bash
        run: |
          python3 scripts/post_buffer_release.py \
            --app-name "$APP_NAME" \
            --tag "$RELEASE_TAG" \
            --release-url "${GITHUB_SERVER_URL}/${GITHUB_REPOSITORY}/releases/tag/${RELEASE_TAG}" \
            --platform "macOS=${{ needs.build-macos.result }}" \
            --platform "Windows=${{ needs.build-windows.result }}" \
            --platform "Linux ARM64=${{ needs.build-linux-arm64.result }}" \
            --platform "Linux x64=${{ needs.build-linux-x64.result }}"
